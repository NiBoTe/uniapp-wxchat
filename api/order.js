const order = '/order/'

// 统一下单
const orderLaunch = `${order}launch`;

// 统一支付接口
const orderPay = `${order}pay`;

// 订单详情
const orderDetail = `${order}detail`;

// 订单列表
const orderList = `${order}list`;

// 查看物流
const queryExpressV2 = `${order}queryExpressV2`;

// 订单取消
const orderCancel = `${order}cancel`;

// 确认收货
const confirmDelivered = `${order}confirmDelivered`;

// 高分教材发货
const teachingMaterialSetExpressInfo = `${order}teachingMaterialSetExpressInfo`;

export {
	orderLaunch,
	orderPay,
	orderDetail,
	orderList,
	queryExpressV2,
	orderCancel,
	confirmDelivered,
	teachingMaterialSetExpressInfo
};
