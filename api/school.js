const university = '/university/'

// 高校分类列表
const classify = `${university}classify`;

// 高校列表
const schoolList = `${university}list`;

// 高校详情
const schoolDetail = `${university}detail`;

export {
	classify,
	schoolList,
	schoolDetail
};
