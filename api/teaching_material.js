const teaching_material = '/teaching_material/'

// 高校分类列表
const myMenuList2 = `${teaching_material}myMenuList2`;

// 发布高分教材
const myAddTeachingMaterial = `${teaching_material}myAddTeachingMaterial`;

// 菜单列表
const menuList = `${teaching_material}menuList`;

// 高分教材列表
const appList = `${teaching_material}appList`;

// 高分教材详情
const getDetail = `${teaching_material}detail`;

// 收藏
const addFavorite = `${teaching_material}addFavorite`;

// 评论
const addComment = `${teaching_material}addComment`;

// 评论列表
const commentList = `${teaching_material}commentList`;

// 获取评价标签
const teachingMaterialCommentTagList = `${teaching_material}teachingMaterialCommentTagList`;

// 发表评价
const confirmCompleted = `${teaching_material}confirmCompleted`;

// 我的高分教材
const myList = `${teaching_material}myList`;

// 我的高分教材详情
const myDetail = `${teaching_material}myDetail`;

// 上架高分教材
const myUpdateSaleState = `${teaching_material}myUpdateSaleState`;

// 删除高分教材
const myDeleteTeachingMaterial = `${teaching_material}myDeleteTeachingMaterial`;

export {
	myMenuList2,
	myAddTeachingMaterial,
	menuList,
	appList,
	getDetail,
	addFavorite,
	addComment,
	commentList,
	teachingMaterialCommentTagList,
	confirmCompleted,
	myList,
	myDetail,
	myUpdateSaleState,
	myDeleteTeachingMaterial
};
