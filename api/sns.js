

const sns = '/sns/'

// 广场-列表
const snsList = `${sns}snsList`;

// 发布动态
const snsSave = `${sns}snsSave`;

// 收藏
const addFavorite =  `${sns}addFavorite`;

// 点赞
const addLike =  `${sns}addLike`;

// 评论
const addComment =  `${sns}addComment`;

// 举报
const snsReportSave = `${sns}snsReportSave`;

// 拉黑
const snsBlackSave = `${sns}snsBlackSave`;

// 详情
const snsDetail = `${sns}snsDetail`;

// 评论列表
const commentList = `${sns}commentList`;

// 关注
const followSnsList = `${sns}followSnsList`;

// 我的动态列表
const mySnsList = `${sns}mySnsList`;

// 更多评论
const getReplyList = `${sns}getReplyList`;

export {
	snsList,
	snsSave,
	addFavorite,
	addLike,
	addComment,
	snsReportSave,
	snsBlackSave,
	snsDetail,
	commentList,
	followSnsList,
	mySnsList,
	getReplyList
};
